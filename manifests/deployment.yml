apiVersion : apps/v1
kind: Deployment
metadata:
  name: "cardata-5836"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "cardata-5836"
  template:
    metadata:
      labels:
        app: "cardata-5836"
    spec:
      containers:
        - name: "cardata-5836"
          image: "qpetraroia.azurecr.io/cardata"
          ports:
          - containerPort: 12345