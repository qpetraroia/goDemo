apiVersion : apps/v1
kind: Deployment
metadata:
  name: "cardata-9831"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "cardata-9831"
  template:
    metadata:
      labels:
        app: "cardata-9831"
    spec:
      containers:
        - name: "cardata-9831"
          image: "qpetraroia.azurecr.io/cardata"
          ports:
          - containerPort: 12345