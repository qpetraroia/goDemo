apiVersion: v1
kind: Service
metadata:
    name: "cardata-575c"
    labels:
        app: "cardata-575c"
spec:
    type: LoadBalancer
    ports:
    - port: 12345
      targetPort: 12345
      protocol: TCP
      name: http
    selector:
        app: "cardata-575c"