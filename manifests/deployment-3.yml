apiVersion : apps/v1
kind: Deployment
metadata:
  name: "cardata-5f62"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "cardata-5f62"
  template:
    metadata:
      labels:
        app: "cardata-5f62"
    spec:
      containers:
        - name: "cardata-5f62"
          image: "qpetraroia.azurecr.io/cardata"
          ports:
          - containerPort: 12345