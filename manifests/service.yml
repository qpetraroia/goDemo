apiVersion: v1
kind: Service
metadata:
    name: "cardata-9831"
    labels:
        app: "cardata-9831"
spec:
    type: LoadBalancer
    ports:
    - port: 12345
      targetPort: 12345
      protocol: TCP
      name: http
    selector:
        app: "cardata-9831"